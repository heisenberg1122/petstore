services:
  - type: web
    name: mypetstore-api
    runtime: java
    plan: free
    rootDir: backend
    buildCommand: mvn -q -DskipTests package
    startCommand: java -jar target/mypetstore-0.0.1-SNAPSHOT.jar
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8080
      - key: DATABASE_URL
        fromDatabase:
          name: mypetstore-db
          property: connectionString
      - key: DATABASE_USERNAME
        fromDatabase:
          name: mypetstore-db
          property: user
      - key: DATABASE_PASSWORD
        fromDatabase:
          name: mypetstore-db
          property: password
      - key: APP_CORS_ALLOWED_ORIGIN
        value: https://mypetstore-web.onrender.com

  - type: static
    name: mypetstore-web
    runtime: static
    plan: free
    rootDir: frontend
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://mypetstore-api.onrender.com
      - key: VITE_ADMIN_PANEL_URL
        value: https://mypetstore-api.onrender.com/admin
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: mypetstore-db
    plan: free
    databaseName: mypetstore
    user: mypetstore
